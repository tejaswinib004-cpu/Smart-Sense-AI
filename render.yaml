services:
  - type: web
    name: smartsenseai
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
